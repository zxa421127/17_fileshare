services:
  backend:
    image: python:3.12
    working_dir: /app
    volumes:
      - ./backend:/app
    command: bash -c "pip install -r requirements.txt && uvicorn app.main:app --host 0.0.0.0"
    ports:
      - "8000:8000"
